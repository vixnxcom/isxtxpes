import React from 'react'
import styles from '../style'

const Vid = () => {
  return (
    <section className={`${styles.flexStart} ${styles.paddingX} flex flex-col`}>

    {/* head section */}
    <div className={`${styles.heading1}`}>
    Ready to Discover How to Live Your Dream Life?

    </div>
    <div className={`${styles.subHeading} mt-2`}>
    Watch the video below to see how a simple decision can change your life forever.
    </div>
 {/* head section */}
 </section>
  )
}

export default Vid
